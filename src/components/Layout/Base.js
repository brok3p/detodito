const Base = props => (
    <div className="wrapper">
        {/* <Header />

        <Sidebar />

        <Offsidebar />

        <section className="section-container">
            { props.children }
        </section>

        <Footer /> */}
    </div>
)

export default Base;